{
  "scripts": {
    "backend": "npm --prefix BACKEND run backend",
    "backend:mysql": "npm --prefix BACKEND run backend:mysql",
    "frontend": "npm --prefix frontend run dev",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "axios": "^1.15.0"
  }
}
