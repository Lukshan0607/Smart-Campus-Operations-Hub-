{
  "name": "smart-campus-backend",
  "private": true,
  "scripts": {
    "prebackend": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$conn=Get-NetTCPConnection -LocalPort 8083 -State Listen -ErrorAction SilentlyContinue; if($conn){$conn | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }}; exit 0\"",
    "backend": "mvnw.cmd -q spring-boot:run",
    "prebackend:verbose": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$conn=Get-NetTCPConnection -LocalPort 8083 -State Listen -ErrorAction SilentlyContinue; if($conn){$conn | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }}; exit 0\"",
    "backend:verbose": "mvnw.cmd spring-boot:run",
    "build": "mvnw.cmd -DskipTests package",
    "test": "mvnw.cmd test"
  }
}
